const incidents=Array.isArray(window.INCIDENTS)?window.INCIDENTS:[];const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const id=new URLSearchParams(location.search).get('id');const i=incidents.find(x=>x.id===id);const root=document.getElementById('incidentDetail');const labels={critica:'Crítica',alta:'Alta',media:'Media',activo:'Activo',investigacion:'En investigación',contenido:'Contenido',resuelto:'Resuelto',seguimiento:'Seguimiento',brecha:'Brecha de datos',vulnerabilidad:'Vulnerabilidad',fraude:'Fraude / phishing',malware:'Malware',otro:'Otro'};const fmt=d=>d?new Intl.DateTimeFormat('es-ES',{day:'numeric',month:'long',year:'numeric'}).format(new Date(`${d}T12:00:00`)):'';if(!i){root.innerHTML='<div class="empty">No se ha encontrado este incidente. <a href="../archivo.html">Consulta el archivo</a>.</div>'}else{document.title=`${i.title} | Alertas CiberSec`;document.querySelector('meta[name="description"]').setAttribute('content',i.lead||i.summary);const changed=i.lastIncidentUpdate&&i.lastIncidentUpdate>i.date;root.innerHTML=`<div class="badges"><span class="badge sev-${esc(i.severity)}">${esc(labels[i.severity]||i.severity)}</span><span class="badge">${esc(labels[i.type]||i.type)}</span><span class="badge state">${esc(labels[i.incidentState]||i.incidentState)}</span>${i.geo==='espana'?'<span class="badge spain">AFECTA A ESPAÑA</span>':''}${changed?'<span class="badge updated-badge">↻ ACTUALIZADO</span>':''}<span class="badge confidence">${esc(i.status)}</span></div><h1>${esc(i.title)}</h1>${i.publishedDate&&i.publishedDate>i.date?`<div class="date-context"><strong>Publicada en Alertas CiberSec el ${esc(fmt(i.publishedDate))}</strong><span>El incidente ocurrió el ${esc(fmt(i.date))}. Mostramos ambas fechas para diferenciar cuándo sucedió de cuándo se incorporó al observatorio.</span></div>`:''}<div class="quick-facts date-explainer"><div><span>FECHA DEL INCIDENTE</span><strong>${esc(fmt(i.date))}</strong><small>Hecho original</small></div><div><span>INFORMACIÓN ACTUALIZADA HASTA</span><strong>${esc(fmt(i.lastIncidentUpdate||i.date))}</strong><small>Último dato material disponible</small></div><div><span>PUBLICADO EN ALERTAS</span><strong>${esc(fmt(i.publishedDate||i.date))}</strong><small>Alta inicial en el observatorio</small></div></div><p class="incident-lead">${esc(i.lead||i.summary)}</p><div class="ten-sec"><div><span>QUÉ HA PASADO</span><p>${esc(i.summary)}</p></div><div><span>¿ME AFECTA?</span><p>${esc(i.affected)}</p></div><div id="acciones"><span>QUÉ HAGO AHORA</span><p>${esc((i.actions||[])[0]||'Consulta las fuentes oficiales.')}</p></div></div>${i.reportedScale?`<div class="scale-callout"><span>${esc(i.reportedScaleStatus)}</span><strong>${esc(i.reportedScale)}</strong></div>`:''}<div class="quick-facts"><div><span>Ámbito</span><strong>${esc(i.scope)}</strong></div><div><span>Datos afectados</span><strong>${esc((i.dataTypes||[]).join(' · ')||'Sin determinar')}</strong></div><div><span>Explotación / técnica</span><strong>${esc(i.exploitation||'Sin determinar')}</strong></div></div><div class="detail-grid"><section class="detail-block"><h2>Por qué importa</h2><p>${esc(i.whyItMatters)}</p></section><section class="detail-block"><h2>Qué hacer</h2><ul class="actions">${(i.actions||[]).map(a=>`<li>${esc(a)}</li>`).join('')}</ul></section></div>${i.timeline?.length?`<section class="timeline"><h2>Cronología</h2>${i.timeline.map(e=>`<div class="timeline-row"><time>${esc(e.date)}</time><p>${esc(e.text)}</p></div>`).join('')}</section>`:''}${i.notes?`<div class="sources"><strong>Qué sabemos y qué no:</strong><p>${esc(i.notes)}</p></div>`:''}<div class="sources"><strong>Fuentes y nivel de procedencia</strong>${(i.sources||[]).map(s=>`<a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer"><small>${esc(s.kind||'Fuente')}</small>${esc(s.label)}</a>`).join('')}</div>`;const canonical=document.createElement('link');canonical.rel='canonical';canonical.href=location.href;document.head.appendChild(canonical);[['og:title',i.title],['og:description',i.lead||i.summary],['og:type','article']].forEach(([p,c])=>{const m=document.createElement('meta');m.setAttribute('property',p);m.content=c;document.head.appendChild(m)});const ld=document.createElement('script');ld.type='application/ld+json';ld.textContent=JSON.stringify({'@context':'https://schema.org','@type':'Article',headline:i.title,datePublished:i.publishedDate||i.date,dateModified:i.lastIncidentUpdate||i.date,description:i.lead||i.summary,about:{'@type':'Thing',name:i.organization}});document.head.appendChild(ld)}